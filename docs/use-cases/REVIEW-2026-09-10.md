# Use-case review — 2026-09-10

## Method

Every `FEATURE_PAGES` key was checked against `DETAILS` in `useCases.ts`. Missing pages used the generic fallback. Those six are now first-class. Existing cases were lifted toward Cockburn fully-dressed and Gherkin with one behaviour per scenario.

## Missing before this pass

`features`, `users`, `userCreate`, `roles`, `roleCreate`, `security`.

## Research applied

- Cockburn: actor intent, not clicks; success vs minimal guarantee.
- BDD: one behaviour per scenario; no UI selectors in Gherkin.
- RBAC vs feature levels: role = verbs; level = product slice.
- SCS-001: AES write switch never disables decrypt.
- Decision A: empty lists stay empty.
- Official HTML remains screen authority.

## Implementation started from the updated cases

1. Research overlay `useCaseResearch.ts` merged into `DEFAULT_USE_CASES`.
2. Markdown export helpers + download routes.
3. Per-page markdown files under `docs/use-cases/`.
4. History table in `HISTORY.md`; runtime history stays on `use_case_revisions`.

## Still open

- Stage / validate / export rule pages still need live binder confirmation on every official control.
- Event framework overlay title mismatch remains an exception in that case.
- Consul/Envoy mesh is specified, not attached, in this environment.
